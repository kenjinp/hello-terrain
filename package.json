{
  "name": "hello-terrain",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter './packages/*' run build && pnpm --filter './apps/*' run dev",
    "build": "pnpm --filter './packages/*' run build && pnpm --filter './apps/*' run build",
    "bench:work:docs": "node scripts/update-work-graph-bench-docs.js",
    "test": "pnpm -r run test",
    "lint": "oxlint .",
    "format": "oxfmt .",
    "commit": "gitmoji -c"
  },
  "devDependencies": {
    "@config/typescript": "workspace:*",
    "gitmoji-cli": "^9.7.0",
    "oxfmt": "^0.21.0",
    "oxlint": "^1.36.0"
  },
  "packageManager": "pnpm@10.27.0",
  "engines": {
    "pnpm": ">=10.0.0",
    "node": ">=24.0.0"
  },
  "references": [
    {
      "path": "./packages/hello-terrain/three"
    },
    {
      "path": "./packages/hello-terrain/react"
    }
  ],
  "publishConfig": {
    "access": "public"
  }
}