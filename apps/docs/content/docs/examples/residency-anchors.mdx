---
title: Residency Anchors
description: Demonstrates keeping gameplay terrain resident while render culling hides tiles
---

import ResidencyAnchorsScene from "@/examples/ResidencyAnchorsScene"

<div className="h-[600px] rounded overflow-hidden">
<ResidencyAnchorsScene />
</div>

## Overview

This example drives terrain visibility from a helper camera, then moves an orange residency anchor around the terrain.

- Blue wireframe tiles are render-visible.
- Orange wire boxes are resident support tiles that are outside the draw set.
- Orange sphere is the anchor region.

With `@hello-terrain/react`, combine a custom culling `camera` with `getResidencyAnchors`:

```tsx
const terrain = useTerrain({
  rootSize: 180,
  maxLevel: 10,
  elevation,
  camera: cullingCamera,
  getResidencyAnchors: () => [
    {
      position: anchorPosition,
      radius: 96,
    },
  ],
});
```

Render visibility still controls the draw list, but query/readback terrain follows the resident set.

At the core API level, anchors are written through `quadtreeUpdate.residency`:

```ts
graph.set(quadtreeUpdate, (prev) => {
  const next = writeUpdateParamsFromCamera(prev, cullingCamera);
  next.residency = {
    anchors: [
      {
        position: playerOrProbePosition,
        radius: 96,
      },
    ],
  };
  return next;
});
```
